---
import '../styles/global.css';
import ScrollingCode from '../components/ScrollingCode.astro';

const row1 = [
  { src: '/smithsonian.jpg', alt: 'NIMH Oral Presentation', label: 'NIMH/NIH Selected Lightning Talk Speaker' },
  { src: '/art.jpg', alt: 'Art and Science', label: '3rd Place Art Winner! "Waves of Consciousness"' },
  { src: '/poster.jpg', alt: 'ASSC Presentation', label: 'Oral presentation at NIMH/NIH' },
  { src: '/talk.jpg', alt: 'Poster Presentation', label: 'Advocating for environmentally friendly infrastructure' },
  { src: '/oxford.jpg', alt: 'Oxford', label: 'Perception of iridescence at the University of Oxford' },
  { src: '/narcan.jpg', alt: 'Narcan', label: '' },
];

// const row2 = [
//   { src: '/Hawaii Pacific Neuroscience summer cohort.jpg', alt: 'Hawaii Pacific Neuroscience summer cohort', label: 'Hawaii Pacific Neuroscience summer cohort' },
//   { src: '/SFIM lab dinner.jpeg', alt: 'SFIM lab dinner', label: 'SFIM lab dinner' },
//   { src: '/Summer internship social in Oahu.jpg', alt: 'Summer internship social in Oahu', label: 'Summer internship social in Oahu' },
//   { src: '/Summer_internship_in_Oahu.jpg', alt: 'Summer internship in Oahu', label: 'Summer internship in Oahu' },
//   { src: '/graduation.jpeg', alt: 'Graduation', label: 'Graduation' },
// ];
---

<!-- Developed by Plyfaa Suwanamalik-Murphy. Last modified: 03/5/2026 -->

<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Plyfaa Suwanamalik-Murphy</title>
    <link rel="preconnect" href="https://fonts.googleapis.com"/>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=EB+Garamond:ital,wght@0,400;0,500;1,400&family=DM+Mono:wght@300;400&display=swap" rel="stylesheet"/>
  </head>

  <body>

    <!-- NAV: Fixed top bar, background present on scroll -->
    <nav id="nav">
      <a href="#about" class="nav-logo">Plyfaa Suwanamalik-Murphy</a>
      <div class="nav-icons" aria-label="Contact">
        <a href="mailto:plysuwana@gmail.com" aria-label="Email" class="nav-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 7l9 6 9-6"/></svg>
        </a>
        <a href="https://www.linkedin.com/in/plyfaa-murphy/" target="_blank" rel="noopener" aria-label="LinkedIn" class="nav-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M20.45 3H3.55A.55.55 0 0 0 3 3.55v16.9c0 .3.25.55.55.55h16.9a.55.55 0 0 0 .55-.55V3.55A.55.55 0 0 0 20.45 3zM8.34 18.34H5.67V9.67h2.67v8.67zM7 8.5a1.55 1.55 0 1 1 0-3.1 1.55 1.55 0 0 1 0 3.1zm11.34 9.84h-2.67v-4.42c0-1.05-.02-2.4-1.46-2.4-1.46 0-1.69 1.14-1.69 2.32v4.5H9.86V9.67h2.56v1.18h.04c.36-.67 1.22-1.38 2.52-1.38 2.7 0 3.2 1.78 3.2 4.08v4.79z"/></svg>
        </a>
        <a href="https://github.com/plyfaa" target="_blank" rel="noopener" aria-label="GitHub" class="nav-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 .5C5.65.5.5 5.65.5 12c0 5.08 3.29 9.39 7.86 10.91.58.11.79-.25.79-.56 0-.27-.01-1-.02-1.95-3.2.69-3.88-1.54-3.88-1.54-.52-1.33-1.28-1.69-1.28-1.69-1.05-.71.08-.7.08-.7 1.16.08 1.77 1.19 1.77 1.19 1.03 1.77 2.71 1.26 3.37.96.1-.75.4-1.26.73-1.55-2.55-.29-5.24-1.28-5.24-5.7 0-1.26.45-2.29 1.19-3.1-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.18 1.18a11.04 11.04 0 0 1 5.79 0c2.21-1.49 3.18-1.18 3.18-1.18.63 1.59.23 2.76.11 3.05.74.81 1.19 1.84 1.19 3.1 0 4.43-2.69 5.41-5.25 5.7.41.35.78 1.05.78 2.12 0 1.53-.01 2.77-.01 3.14 0 .31.21.67.8.56C20.21 21.39 23.5 17.08 23.5 12 23.5 5.65 18.35.5 12 .5z"/></svg>
        </a>
        <a href="https://scholar.google.com/citations?user=TCRiqYcAAAAJ&hl=en" target="_blank" rel="noopener" aria-label="Google Scholar" class="nav-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"><path d="M12 3L2 8l10 5 10-5-10-5z"/><path d="M6 10.5v4c0 1.66 2.7 3 6 3s6-1.34 6-3v-4"/></svg>
        </a>
        <a href="/cv.pdf" target="_blank" rel="noopener" aria-label="Curriculum Vitae" class="nav-icon nav-icon--with-text">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/><line x1="8" y1="13" x2="16" y2="13"/><line x1="8" y1="17" x2="13" y2="17"/></svg>
          <span>CV</span>
        </a>
      </div>
      {/* <ul class="nav-links">
        <li><a href="#gallery">From the Lab</a></li>
      </ul> */}
    </nav>

    <!-- ABOUT + CONTACT wrapper -->
    <div class="code-backdrop-wrap">

    <!-- ABOUT: Bio + photo -->
    <section id="about">
<div class="about-text-body">
          <p class="about-body">
            I'm fascinated by the eyes and what they reveal about our perception!
          </p>
          <p class="about-body">
            I'm an incoming PhD student in
            <a href="https://bradylab.ucsd.edu" class="about-highlight" target="_blank">Dr. Timothy Brady's Visual and Memory Lab</a> at
            <a href="https://ucsd.edu" class="about-highlight" target="_blank">UC San Diego</a>. Currently, I'm a Research Fellow at the
            <a href="https://www.nih.gov" class="about-highlight" target="_blank">National Institutes of Health</a> in
            <a href="https://fim.nimh.nih.gov/" class="about-highlight" target="_blank">Dr. Peter Bandettini's lab</a>, where I'm investigating how we can control
            pupil size in real time to improve our perception. Previously, I built
            decoding pipelines for BCIs as a
            <a href="https://www.simonsfoundation.org" class="about-highlight" target="_blank">Simons Foundation Fellow</a> under
            <a href="https://neuroprosthetics.science/" class="about-highlight" target="_blank">Dr. Sergey Stavisky</a>, and investigated visual perception at the
            <a href="https://www.psy.ox.ac.uk/" class="about-highlight" target="_blank">University of Oxford</a> advised by
            <a href="https://www.psy.ox.ac.uk/people/hannah-smithson" class="about-highlight" target="_blank">Dr. Hannah Smithson</a>.
          </p>
          <p class="about-body">
            I completed my Bachelor's in Cognitive Science from
            <a href="https://ucdavis.edu" class="about-highlight" target="_blank">UC Davis</a> in 2 years. And spent most of that time in
            <a href="/cv.pdf" class="about-highlight" target="_blank">research labs</a>, learning as much as I could about eye-tracking, EEG, and visual cognition.
          </p>
      </div>
      <div class="bio-stack">
        <div class="bio-photo-block">
          <div class="bio-photo-wrapper">
            <img src="/smithsonian.jpg" alt="Plyfaa Suwanamalik-Murphy" class="about-photo bio-photo"/>
          </div>
          <span class="bio-photo-caption">NIH/NIMH IRP Fellows’ Scientific Training Day 2025</span>
        </div>
      </div>
    </section>

    {/* EXPERIENCES: Radial orbit — commented out for now
    <section id="experiences">
      <div class="orbit-wrap reveal">
        <div class="orbit" id="orbit">
          <div class="orbit-item" style="--angle: 0deg"></div>
          <div class="orbit-item" style="--angle: 90deg"></div>
          <div class="orbit-item" style="--angle: 180deg"></div>
          <div class="orbit-item" style="--angle: 270deg"></div>
        </div>
        <span class="orbit-center">Projects</span>
        <span class="orbit-note">In progress! (May 2026)</span>
      </div>
    </section>
    */}

    {/* GALLERY: two scrolling rows — commented out for now
    <section id="gallery">
      <p class="section-tag reveal gallery-tag">From the Lab</p>
      <div class="gallery-marquee reveal">
        <div class="gallery-row">
          <div class="gallery-track scroll-left">
            {[...row1, ...row1].map(p => (
              <figure class="gallery-item">
                <img src={p.src} alt={p.alt} />
                {p.label && <figcaption class="gallery-item-label">{p.label}</figcaption>}
              </figure>
            ))}
          </div>
        </div>
        <div class="gallery-row">
          <div class="gallery-track scroll-right">
            {[...row2, ...row2].map(p => (
              <figure class="gallery-item">
                <img src={p.src} alt={p.alt} />
                {p.label && <figcaption class="gallery-item-label">{p.label}</figcaption>}
              </figure>
            ))}
          </div>
        </div>
      </div>
    </section>
    */}


    <footer>
      <p class="footer-credit">
        Built with <a href="https://astro.build" class="footer-highlight" target="_blank">Astro</a>, set in
        <a href="https://fonts.google.com/specimen/Cormorant+Garamond" class="footer-highlight" target="_blank">Cormorant Garamond</a> &amp;
        <a href="https://fonts.google.com/specimen/EB+Garamond" class="footer-highlight" target="_blank">EB Garamond</a>,
        and deployed on <a href="https://pages.github.com" class="footer-highlight" target="_blank">GitHub Pages</a>.
      </p>
    </footer>

    <!-- RESEARCH DETAIL: Opens as full-page overlay -->
    <div class="research-modal-overlay" id="research-modal">
      <div class="research-modal">
        <button class="modal-close" id="modal-close" aria-label="Close">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
            <line x1="18" y1="6" x2="6" y2="18"></line>
            <line x1="6" y1="6" x2="18" y2="18"></line>
          </svg>
        </button>
        <div class="modal-content" id="modal-content">
          <!-- Dynamically populated by JS -->
        </div>
      </div>
    </div>

    <style is:global>
      /* ── DESIGN TOKENS ("this is my color") ── */
      :root {
        /* ── fonts ───────────────────────────────────────── */
        --serif:        'Cormorant Garamond', 'EB Garamond', Georgia, 'Times New Roman', serif;
        --serif-body:   'EB Garamond', Georgia, 'Times New Roman', serif;
        --sans:         'EB Garamond', Georgia, 'Times New Roman', serif;
        --mono:         'EB Garamond', Georgia, 'Times New Roman', serif;
        --code:         'DM Mono', ui-monospace, SFMono-Regular, Menlo, monospace;

        /* ── colors (warm editorial neutrals) ───────────── */
        --bg:           #FFFFFF;
        --surface:      #FCEEE2;
        --text:         #1A1A1A;
        --muted:        rgba(26,26,26,0.55);
        --border:       rgba(26,26,26,0.12);
        --link:         #1A1A1A;
        --accent:       #D6826E;
        --dark-accent:  #0B125F;
        --light-accent: #FCEEE2;
        --blue:         #D6826E;
        --blue-dim:     rgba(214,130,110,0.5);

        /* ── layout & rhythm ────────────────────────────── */
        --maxw:         680px;            /* readable measure for body */
        --section-maxw: 900px;            /* slightly wider container for sections */
        --space-1:      8px;
        --space-2:      16px;
        --space-3:      24px;
        --space-4:      40px;
        --space-5:      64px;
        --space-6:      96px;
        --space-7:      128px;            /* generous breathing room above sections */
        --radius:       0;

        /* ── type sizes (editorial scale) ───────────────── */
        --text-base:    18px;
        --text-sm:      14px;
        --leading-body: 1.7;
        --leading-tight:1.2;
        --heading-section: clamp(22px, 2.6vw, 30px);
        --heading-display: clamp(32px, 4vw, 48px);
      }

      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      html { scroll-behavior: smooth; }
      body { font-family: var(--serif-body); background: var(--bg); color: var(--text); font-size: var(--text-base); line-height: var(--leading-body); font-weight: 400; letter-spacing: 0; overflow-x: hidden; -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
      @media (min-width: 1024px) { html { zoom: 0.85; } }

      /* ── CURSOR PIXELS ── */
      @keyframes pixelRise {
        from { opacity: 1; }
        to { opacity: 0; transform: translate(calc(-50% + var(--dx, 0px)), calc(-50% - 48px)); }
      }

      /* ── NAV ── */
      nav {
        position: static; z-index: 100;
        display: flex; flex-direction: row; justify-content: space-between; align-items: baseline;
        gap: clamp(16px, 3vw, 48px);
        padding: clamp(12px, 2vw, 24px) clamp(20px, 5vw, 64px); border-bottom: none;
        background: transparent;
      }
      .nav-logo { font-family: var(--serif); font-size: clamp(28px, 2.8vw, 42px); font-weight: 400; color: var(--text); text-decoration: none; white-space: nowrap; letter-spacing: 0; }
      .nav-icons { display: flex; align-items: center; gap: clamp(10px, 1.2vw, 18px); margin-top: 2px; }
      .nav-icon { display: inline-flex; align-items: center; justify-content: center; gap: 6px; color: var(--muted); line-height: 0; text-decoration: none; }
      .nav-icon:hover { color: var(--accent); }
      .nav-icon svg { width: clamp(20px, 1.8vw, 26px); height: clamp(20px, 1.8vw, 26px); display: block; }
      .nav-icon--with-text { line-height: 1; }
      .nav-icon--with-text span { font-family: var(--serif-body); font-style: italic; font-size: clamp(15px, 1.2vw, 18px); letter-spacing: 0; }
      .nav-links { display: flex; gap: clamp(16px, 2.4vw, 32px); list-style: none; }
      .nav-links a { font-family: var(--serif-body); font-size: clamp(14px, 1.05vw, 17px); letter-spacing: 0; color: var(--text); text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 4px; text-decoration-color: var(--border); white-space: nowrap; }

      /* ── SECTION BASE ── */
      section { max-width: var(--section-maxw); margin-left: auto; margin-right: auto; padding: var(--space-7) clamp(20px, 5vw, 64px) var(--space-6); border-bottom: 1px solid var(--border); }
      .section-tag { font-family: var(--serif); font-size: var(--heading-section); font-weight: 400; letter-spacing: 0; color: var(--text); margin-bottom: var(--space-5); display: flex; align-items: center; gap: 16px; }
      /* removed the decorative blue line next to section headers */
      .section-tag::after { display: none; }

      /* ── ABOUT ── */
      #about {
        padding-top: var(--space-6); padding-bottom: var(--space-7);
        padding-left: clamp(12px, 2.5vw, 32px); padding-right: clamp(12px, 2.5vw, 32px);
        border-bottom: 1px solid var(--border); min-height: auto; position: relative; color: var(--text);
        display: flex; flex-direction: row; align-items: center; gap: var(--space-5);
        max-width: 1200px; margin-left: auto; margin-right: auto;
      }
      #about::before {
        content: ''; position: absolute; z-index: -2; top: 0; bottom: 0; left: calc(50% - 50vw); width: 100vw;
        background: var(--bg);
      }
      #experiences, #gallery { border-bottom: none; }
      #gallery { padding-bottom: clamp(8px, 1vw, 16px); }
      .about-text-body { position: relative; flex: 1 1 0; min-width: 0; max-width: var(--maxw); margin: 0; border: none; padding: 0; background: transparent; z-index: 2; }
      .bio-stack { position: relative; display: flex; flex-direction: column; align-items: flex-start; gap: clamp(8px, 1.2vw, 14px); z-index: 2; order: -1; flex-shrink: 0; }
      .speech-bubble { background: #FFFFFF; color: #000; padding: clamp(10px, 1.5vw, 16px) clamp(14px, 2.5vw, 22px); border-radius: 18px; position: relative; max-width: 320px; font-family: var(--sans); font-size: clamp(13px, 1.3vw, 18px); }
      .speech-bubble::after { content: ''; position: absolute; bottom: -10px; left: 50%; transform: translateX(-50%); border-left: 10px solid transparent; border-right: 10px solid transparent; border-top: 10px solid #FFFFFF; }
      .about-text-body .about-body:last-child { margin-bottom: 0; }
      .about-name { font-family: var(--serif); font-size: var(--heading-display); font-weight: 400; line-height: var(--leading-tight); letter-spacing: 0; color: var(--text); margin-bottom: var(--space-4); }
      .about-name em { font-style: italic; color: var(--accent); }
      .about-body { font-family: var(--serif-body); font-size: var(--text-base); font-weight: 400; color: var(--text); line-height: var(--leading-body); letter-spacing: 0; margin-bottom: var(--space-3); max-width: var(--maxw); }
      .bio-photo-wrapper {
        position: relative; flex-shrink: 0;
        width: clamp(340px, 42vw, 560px);
        overflow: hidden;
        border-radius: 10px;
      }
      .about-photo { position: relative; z-index: 1; width: 100%; height: auto; object-fit: unset; object-position: center; border-radius: 0; display: block; transform: none; transition: transform 0.5s; }
      .bio-photo-block { display: flex; flex-direction: column; align-items: flex-start; width: clamp(340px, 42vw, 560px); }
      .hero-separator { order: -1; font-family: var(--mono); font-size: clamp(14px, 1.6vw, 22px); color: rgba(26,26,24,0.55); letter-spacing: 8px; margin: clamp(6px, 0.9vw, 12px) 0; text-align: center; line-height: 1; }
      .section-separator { font-family: var(--mono); font-size: clamp(14px, 1.6vw, 22px); color: rgba(255,255,255,0.6); letter-spacing: 8px; text-align: center; padding: clamp(24px, 4vw, 48px) 0; }
      .bio-photo-caption { display: inline-flex; align-items: center; gap: 6px; font-family: var(--serif-body); font-style: italic; font-size: clamp(13px, 1vw, 16px); letter-spacing: 0; color: var(--muted); margin-top: 8px; }
      .bio-pin { font-size: 1.1em; line-height: 1; }
      .about-photo-label { font-family: var(--mono); font-size: clamp(8px, 0.8vw, 10px); letter-spacing: 2px; color: rgba(255,255,255,0.6); text-align: right; margin-top: 10px; }
      .about-highlight { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; text-decoration-thickness: 1px; text-decoration-color: var(--accent); }
      .about-highlight:hover { color: var(--accent); text-decoration-color: var(--accent); }

      /* ── EXPERIENCES (ORBIT) ── */
      #experiences { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; padding-top: clamp(80px, 12vw, 160px); padding-bottom: clamp(80px, 12vw, 160px); background: #FFFFFF; color: #000000; position: relative; z-index: 0; }
      #experiences::before { content: ''; position: absolute; z-index: -1; top: 0; bottom: 0; left: calc(50% - 50vw); width: 100vw; background: #FFFFFF; }
      #experiences .orbit-center { color: #000000; }
      #experiences .orbit-note { color: rgba(0,0,0,0.6); }
      #experiences .orbit-item { background: rgba(0,0,0,0.08); border-color: rgba(0,0,0,0.3); }
      .orbit-wrap { padding: clamp(16px, 2.5vw, 36px) 0; display: flex; justify-content: center; width: 100%; position: relative; perspective: 1400px; perspective-origin: center; }
      .orbit { position: relative; width: clamp(420px, 70vw, 820px); height: clamp(420px, 70vw, 820px); will-change: transform; }
      .orbit-center { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); font-family: var(--sans); font-size: clamp(28px, 4vw, 56px); color: var(--text); letter-spacing: 2px; pointer-events: none; }
      .orbit-note { position: absolute; top: calc(50% + clamp(28px, 4vw, 50px)); left: 50%; transform: translate(-50%, 0); font-family: var(--mono); font-size: clamp(11px, 1.1vw, 14px); color: rgba(255,255,255,0.6); letter-spacing: 1px; pointer-events: none; white-space: nowrap; }
      .orbit-item {
        position: absolute; top: 50%; left: 50%;
        width: clamp(110px, 15vw, 180px); height: clamp(110px, 15vw, 180px);
        background: rgba(255,255,255,0.12);
        border: 1px solid rgba(255,255,255,0.35);
        border-radius: 50%;
        transform: translate(-50%, -50%) rotate(var(--angle)) translateY(calc(-1 * clamp(140px, 23vw, 260px))) rotate(calc(-1 * var(--angle)));
      }
      @media (prefers-reduced-motion: reduce) { .orbit { transform: none !important; } }

      /* ── GALLERY ── */
      #gallery .section-tag { font-family: var(--serif); justify-content: center; text-align: center; font-size: var(--heading-section); font-weight: 400; letter-spacing: 0; color: var(--text); }
      #gallery .section-tag.reveal { opacity: 1; transform: none; }
      #gallery .section-tag .slide-from-left { display: inline-block; will-change: transform; }
      .gallery-marquee { display: flex; flex-direction: column; gap: clamp(12px, 1.5vw, 20px); width: 100vw; margin-left: calc(50% - 50vw); margin-right: calc(50% - 50vw); }
      .gallery-row { overflow-x: auto; overflow-y: hidden; scrollbar-width: none; -ms-overflow-style: none; }
      .gallery-row::-webkit-scrollbar { display: none; }
      .gallery-track { display: flex; align-items: center; gap: clamp(12px, 1.5vw, 20px); width: max-content; }
      .gallery-track.scroll-left  { animation: gallery-scroll-left 90s linear infinite; }
      .gallery-track.scroll-right { animation: gallery-scroll-right 90s linear infinite; }
      .gallery-row:hover .gallery-track { animation-play-state: paused; }
      .gallery-item { margin: 0; flex-shrink: 0; width: clamp(220px, 24vw, 320px); aspect-ratio: 4/3; background: var(--surface); overflow: hidden; position: relative; }
      .gallery-item img { width: 100%; height: 100%; object-fit: cover; display: block; }
      .gallery-item-label { position: absolute; bottom: 12px; left: 12px; font-family: var(--serif-body); font-style: italic; font-size: clamp(11px, 0.85vw, 13px); letter-spacing: 0; color: #FFFFFF; background: rgba(0,0,0,0.5); padding: 3px 8px; }
      @keyframes gallery-scroll-left  { from { transform: translateX(0); }     to { transform: translateX(-50%); } }
      @keyframes gallery-scroll-right { from { transform: translateX(-50%); }  to { transform: translateX(0); }    }
      @media (prefers-reduced-motion: reduce) { .gallery-track { animation: none; } }


      /* ── CONTACT ── */
      #contact { min-height: auto; display: flex; flex-direction: column; align-items: center; text-align: center; gap: var(--space-4); color: var(--text); position: relative; z-index: 0; }
      #contact::before { content: ''; position: absolute; z-index: -2; top: 0; bottom: 0; left: calc(50% - 50vw); width: 100vw; background: var(--bg); }
      #contact .section-tag { font-family: var(--serif); color: var(--text); font-size: var(--heading-section); font-weight: 400; letter-spacing: 0; margin-bottom: 0; justify-content: center; text-align: center; }
      #contact .section-tag.reveal { opacity: 1; transform: none; }
      #contact .section-tag .slide-from-right { display: inline-block; will-change: transform; }
      #contact .contact-body { color: var(--muted); }
      #contact .contact-email { color: var(--text); }
      #contact .contact-link { color: var(--muted); }
      #contact .contact-email:hover, #contact .contact-link:hover { color: var(--accent); }
      #contact .section-tag { justify-content: center; }
      .contact-body { font-size: clamp(13px, 1.2vw, 16px); font-weight: 300; color: rgba(255,255,255,0.75); line-height: 1.7; max-width: 480px; margin: 0; }
      .contact-email { font-family: var(--serif-body); font-size: var(--text-base); letter-spacing: 0; color: var(--text); text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 4px; text-decoration-color: var(--border); display: inline-block; }
      .contact-email:hover { color: var(--accent); text-decoration-color: var(--accent); }
      .contact-links { display: flex; flex-direction: column; align-items: center; padding-top: 0; gap: clamp(10px, 1.6vw, 18px); }
      .contact-link { display: inline-flex; justify-content: center; align-items: center; gap: 12px; padding: 0; text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 4px; text-decoration-color: var(--border); color: var(--muted); font-family: var(--serif-body); font-size: var(--text-base); letter-spacing: 0; }
      .contact-link:hover { color: var(--accent); text-decoration-color: var(--accent); }
      .contact-link-arrow { display: none; }

      /* ── FOOTER ── */
      footer { max-width: none; margin: 0; padding: var(--space-4) clamp(20px, 5vw, 64px); display: flex; justify-content: flex-start; align-items: flex-start; border-top: 1px solid var(--border); }
      .footer-credit { font-family: var(--serif-body); font-style: italic; font-size: clamp(13px, 1vw, 16px); letter-spacing: 0; color: var(--muted); line-height: 1.8; max-width: 800px; }
      .footer-highlight { color: var(--text); text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 3px; text-decoration-color: var(--border); }
      .footer-highlight:hover { color: var(--accent); text-decoration-color: var(--accent); }
      a.footer-highlight:hover { color: var(--text); }

      /* ── RESEARCH MODAL ── */
      .research-modal-overlay {
        position: fixed; inset: 0; z-index: 200;
        background: rgba(0, 0, 0, 0.0); backdrop-filter: blur(0px);
        pointer-events: none; opacity: 0; overflow-y: auto;
        transition: background 0.4s ease, backdrop-filter 0.4s ease, opacity 0.4s ease;
      }
      .research-modal-overlay.open {
        opacity: 1; pointer-events: all;
        background: rgba(0, 0, 0, 0.92); backdrop-filter: blur(20px);
      }
      .research-modal {
        max-width: 860px; margin: 0 auto; padding: clamp(60px, 8vw, 100px) clamp(20px, 5vw, 64px) clamp(40px, 6vw, 80px);
        transform: translateY(40px); opacity: 0;
        transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.5s ease;
      }
      .research-modal-overlay.open .research-modal { transform: translateY(0); opacity: 1; }
      .modal-close {
        position: fixed; top: clamp(16px, 2.5vw, 32px); right: clamp(20px, 3vw, 40px);
        background: none; border: none; color: var(--text);
        width: clamp(36px, 5vw, 48px); height: clamp(36px, 5vw, 48px);
        display: flex; align-items: center; justify-content: center;
        transition: color 0.2s; z-index: 201;
      }
      .modal-close:hover { color: var(--blue); }
      .modal-content .modal-tag { font-family: var(--mono); font-size: clamp(10px, 1vw, 14px); letter-spacing: 4px; color: var(--blue); display: flex; align-items: center; gap: 16px; margin-bottom: 24px; }
      /* hide line adjacent to modal tag */
      .modal-content .modal-tag::after { display: none; }
      .modal-content .modal-heading { font-family: var(--serif); font-size: clamp(22px, 2.7vw, 35px); color: var(--text); line-height: 1.15; margin-bottom: 16px; }
      .modal-content .modal-section-label { font-family: var(--mono); font-size: clamp(10px, 1vw, 14px); letter-spacing: 3px; color: var(--blue); margin-bottom: 16px; margin-top: 56px; }
      .modal-content .modal-body { font-size: clamp(13px, 1.1vw, 15px); font-weight: 300; color: rgba(255,255,255,0.75); line-height: 1.9; margin-bottom: 16px; max-width: 680px; }
      .modal-content .modal-image-grid img { width: 100%; aspect-ratio: 16/10; object-fit: cover; border: 1px solid var(--border); border-radius: 2px; display: block; filter: grayscale(15%) brightness(0.9); transition: filter 0.4s; }
      .modal-body-grid { display: grid; grid-template-columns: 1fr 160px; gap: clamp(24px, 3.5vw, 48px); margin-top: 32px; }
      .modal-body-main { min-width: 0; }
      .modal-sidebar { border-left: 1px solid var(--border); padding-left: 24px; }
      .modal-sidebar-item { margin-bottom: 20px; }
      .modal-sidebar-label { display: block; font-family: var(--mono); font-size: clamp(10px, 1vw, 14px); letter-spacing: 2px; color: var(--blue-dim); margin-bottom: 4px; }
      .modal-sidebar-value { display: block; font-family: var(--mono); font-size: clamp(10px, 0.95vw, 13px); color: rgba(255,255,255,0.75); font-weight: 300; }
      .modal-file-list { display: flex; flex-direction: column; }
      .modal-file-item { display: flex; align-items: center; gap: 10px; padding: 14px 0; text-decoration: none; transition: background 0.2s; }
      .modal-file-icon { font-size: 30px; }
      .modal-file-name { font-family: var(--mono); font-size: clamp(11px, 1vw, 14px); letter-spacing: 1px; color: rgba(255,255,255,0.75); flex: 1; }
      .modal-file-open { font-family: var(--mono); font-size: 9px; letter-spacing: 2px; color: var(--blue); opacity: 0; transition: opacity 0.2s; }
      .modal-file-item:hover .modal-file-open { opacity: 1; }
      .modal-file-item:hover .modal-file-name { color: var(--text); }
      body.modal-open { overflow: hidden; }

      /* ── ANIMATIONS ── */
      .reveal { opacity: 0; transform: translateY(24px); transition: opacity 0.7s ease, transform 0.7s ease; }
      .reveal.visible { opacity: 1; transform: none; }

      /* ── RESPONSIVE: SMALL LAPTOPS / TABLETS ── */
      @media (max-width: 900px) {
        /* Hide the scrolling code backdrop on smaller devices */
        .code-viz--backdrop { display: none; }

        /* Pull in heavy side padding from the about section */
        #about {
          padding-left: clamp(24px, 5vw, 48px);
          padding-right: clamp(24px, 5vw, 48px);
          padding-top: clamp(32px, 6vw, 56px);
          padding-bottom: clamp(32px, 6vw, 56px);
          align-items: center;
          min-height: auto;
        }
        /* Trim nav side padding so it doesn't crowd content */
        nav { padding: clamp(12px, 2vw, 20px) clamp(20px, 4vw, 40px); }

        /* Stack hero so photo + text don't collide */
        .about-text-body { position: static; transform: none; width: 100%; max-width: 640px; margin: 0 auto; }
        .bio-stack { position: static; align-items: center; margin-top: clamp(24px, 4vw, 40px); width: 100%; }
        .bio-photo-block { align-items: center; width: clamp(220px, 60vw, 360px); }
        .bio-photo-wrapper { width: 100%; aspect-ratio: auto; }
        .bio-photo-caption { text-align: center; }

        .about-text-body::before, .about-text-body::after { display: none; }

        .modal-body-grid { grid-template-columns: 1fr; }
        .modal-sidebar { border-left: none; padding-left: 0; border-top: 1px solid var(--border); padding-top: 24px; display: flex; flex-wrap: wrap; gap: 16px 32px; }
      }

      /* ── RESPONSIVE: PHONES ── */
      @media (max-width: 600px) {
        /* Disable custom cursor on touch devices */
        .cursor, .cursor-ring { display: none; }

        /* Mobile: stack normally — text on top, photo at bottom */
        .about-text-body { position: static; width: 100%; max-width: 100%; }
        .bio-stack { position: static; align-items: center; margin-top: clamp(20px, 4vw, 40px); }
        .bio-photo-wrapper { width: clamp(200px, 80vw, 320px); aspect-ratio: auto; }
        .about-text-body .about-body { background: transparent; color: var(--text); padding: 0; border-radius: 0; }
        .about-text-body .about-highlight { color: var(--link); }

        .contact-links { padding-top: 0; }

        /* Modal sidebar stacks */
        .modal-body-grid { grid-template-columns: 1fr; }
        .modal-sidebar { border-left: none; padding-left: 0; border-top: 1px solid var(--border); padding-top: 24px; display: flex; flex-wrap: wrap; gap: 12px 24px; }

        /* Nav: stack logo above links so a long name doesn't collide */
        nav {
          flex-direction: column;
          align-items: flex-start;
          gap: 8px;
          padding: 12px clamp(16px, 5vw, 24px);
        }
        .nav-logo { font-size: 13px; letter-spacing: 1.5px; white-space: normal; }
        .nav-links { gap: 6px 12px; flex-wrap: wrap; justify-content: flex-start; width: 100%; }
        .nav-links a { font-size: 11px; letter-spacing: 1px; }
        .nav-links .dot { opacity: 0.5; }
      }

      /* ── RESPONSIVE: VERY SMALL PHONES ── */
      @media (max-width: 400px) {
        .nav-logo { font-size: 12px; letter-spacing: 1px; }
        .nav-links a { font-size: 10px; letter-spacing: 0.5px; }
        .nav-links { gap: 4px 8px; }
      }
    </style>

    <script>
// ── EXPERIENCES ORBIT: scroll-driven rotation + tilt ─────
      const orbit = document.getElementById('orbit');
      if (orbit) {
        const orbitContainer = orbit.parentElement;
        const updateOrbit = () => {
          const rect = orbitContainer.getBoundingClientRect();
          const vh = window.innerHeight;
          const progress = (vh - rect.top) / (vh + rect.height);
          const clamped = Math.max(0, Math.min(1, progress));
          // Z spin: 120° across full visible range (slower than before)
          const zRot = clamped * 120;
          // X tilt: starts at 70° (angled), eases to 0° by mid-scroll, stays facing
          const xRot = clamped < 0.5 ? 70 * (1 - clamped * 2) : 0;
          orbit.style.transform = `rotateX(${xRot}deg) rotate(${zRot}deg)`;
        };
        window.addEventListener('scroll', updateOrbit, { passive: true });
        window.addEventListener('resize', updateOrbit);
        updateOrbit();
      }

      // ── GALLERY + CONTACT TAGS: one-time rise-up on enter ───
      const riseObserver = new IntersectionObserver(entries => {
        entries.forEach(({ target, isIntersecting }) => {
          if (isIntersecting) {
            target.classList.add('risen');
            riseObserver.unobserve(target);
          }
        });
      }, { threshold: 0.2 });
      document.querySelectorAll('.gallery-tag, .contact-tag').forEach(el => riseObserver.observe(el));

// ── SCROLL REVEAL ─────────────────────────────────────────
      const revealObserver = new IntersectionObserver(entries => {
        entries.forEach(({ target, isIntersecting }) => {
          if (isIntersecting) {
            target.classList.add('visible');
            revealObserver.unobserve(target);
          }
        });
      }, { threshold: 0.1 });

      document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

      // ── RESEARCH DETAIL MODAL (JS) ─────────────────────────────────
      const projectData = {
        'semantic-congruency': {
          tag: '',
          title: 'Semantic Congruency Effects on Visual Attention',
          sidebar: {},
          overview: `Visual search is an everyday activity where humans effectively integrate
          multiple sources of information to find relevant objects. Recent evidence
          shows that semantic congruency, how well an object fits within its scene
          context, drives overt attention. When viewing a scene, we rapidly extract its
          gist, which activates schemas encoding expectations about which objects are
          likely to appear. While much has been revealed about recognition of
          individual objects in isolation, the neural mechanisms underlying attention to
          congruent versus incongruent objects within naturalistic scenes remain poorly
          understood.`,
          methods: `Aim 1 develops an open-source dataset of ~450 scenes with systematically 
          graded congruency levels (low, medium, high). Aim 2 uses eye-tracking and EEG to 
          adjudicate between the facilitation and interference models explaining semantic 
          congruency effects on visual attention.`,
          files: [
            { name: 'NSF GRFP Research Proposal', path: 'https://drive.google.com/file/d/1wOT4NLdTZXYkIw26foogs5lxleW_BVvL/view?usp=sharing', type: 'pdf' },
          ],
        },
      };

      const modal = document.getElementById('research-modal');
      const modalContent = document.getElementById('modal-content');
      const modalClose = document.getElementById('modal-close');

      function openProject(projectId) {
        const data = projectData[projectId];
        if (!data) return;

        const s = data.sidebar;
        const sidebarItems = [
          { label: 'Institution', value: s.institution },
          { label: 'Advisor', value: s.advisor },
          { label: 'Duration', value: s.duration },
          { label: 'Funding', value: s.funding },
          { label: 'Funder', value: s.funder },
        ];

        const sidebarHTML = sidebarItems.filter(item => item.value).map(item => `
          <div class="modal-sidebar-item">
            <span class="modal-sidebar-label">${item.label}</span>
            <span class="modal-sidebar-value">${item.value}</span>
          </div>
        `).join('');

        const files = data.files?.filter(f => f.path) || [];
        const filesHTML = files.length > 0 ? `
          <p class="modal-section-label">Documents</p>
          <div class="modal-file-list">
            ${files.map(f => `
              <a href="${f.path}" target="_blank" class="modal-file-item">
                <span class="modal-file-icon">${f.type === 'pdf' ? '📄' : '🖼'}</span>
                <span class="modal-file-name">${f.name}</span>
                <span class="modal-file-open">Open →</span>
              </a>
            `).join('')}
          </div>
        ` : '';

        modalContent.innerHTML = `
          ${data.tag ? `<p class="modal-tag">${data.tag}</p>` : ''}
          <h2 class="modal-heading">${data.title}</h2>
          <div class="modal-body-grid">
            <div class="modal-body-main">
              <p class="modal-section-label">Overview</p>
              <p class="modal-body">${data.overview}</p>
              <p class="modal-section-label">Methods</p>
              <p class="modal-body">${data.methods}</p>
              ${filesHTML}
            </div>
            ${sidebarHTML ? `<div class="modal-sidebar">${sidebarHTML}</div>` : ''}
          </div>
        `;

        document.body.classList.add('modal-open');
        modal.classList.add('open');
      }

      function closeProject() {
        modal.classList.remove('open');
        document.body.classList.remove('modal-open');
      }

      document.querySelectorAll('.research-item[data-project]').forEach(item => {
        item.addEventListener('click', () => openProject(item.dataset.project));
        item.addEventListener('keydown', e => {
          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); openProject(item.dataset.project); }
        });
      });

      modalClose.addEventListener('click', closeProject);
      modal.addEventListener('click', e => { if (e.target === modal) closeProject(); });
      document.addEventListener('keydown', e => { if (e.key === 'Escape') closeProject(); });
    </script>

  </body> 
</html>
